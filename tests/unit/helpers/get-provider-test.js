import { getProvider } from 'osf-meetings/helpers/get-provider';
import { module, test } from 'qunit';

module('Unit | Helper | get provider');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getProvider([42]);
  assert.ok(result);
});
