import { getUploadsUrl } from 'osf-meetings/helpers/get-uploads-url';
import { module, test } from 'qunit';

module('Unit | Helper | get uploads url');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getUploadsUrl([42]);
  assert.ok(result);
});
